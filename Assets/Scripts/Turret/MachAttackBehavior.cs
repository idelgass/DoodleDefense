using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// New class for dealing with spread and other new stuff introduced with the new turret design

public class MachAttackBehavior : ProjectileAttackBehavior
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
